

const TitleCom = ({title}) => {
    return (
        <div className=" flex justify-center items-center">
            <h2 className="text-4xl border-4 border-blue-500 py-2 px-6 
            rounded-tr-3xl rounded-bl-3xl uppercase font-bold mb-8 text-center">{title}</h2>
        </div>
    );
};

export default TitleCom;